# System Configuration Branch

This branch contains root-level system configuration files.
Managed by chezmoi running as root.

## Steps to Setup
1. Add SSH public & private keys to root's home
2. Add GNUPG keys to root's home + Copy related configs
3. Setup `.gitconfig` for root user
