# System Configuration Branch

This branch contains root-level system configuration files.
Managed by chezmoi running as root.

## Steps to Setup
1. Add SSH public & private keys to root directory
2. Setup `.gitconfig` for root user
3. Add GNUPG keys to root directory + Copy configs
