#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
[ -r /home/x0rzavi/.byobu/prompt ] && . /home/x0rzavi/.byobu/prompt   #byobu-prompt#
